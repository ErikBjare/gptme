apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: gptme-ingress
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  ingressClassName: nginx
  rules:
  - host: gptme.local
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: gptme
            port:
              number: 8080
