apiVersion: v1
kind: Service
metadata:
  name: gptme
  labels:
    app: gptme
spec:
  type: ClusterIP
  ports:
  - port: 8080
    targetPort: 5000
    protocol: TCP
    name: http
  selector:
    app: gptme
