apiVersion: v1
kind: Service
metadata:
  name: gptme-agent-service
spec:
  type: ClusterIP
  selector:
    app: gptme-agent-label
  ports:
  - port: 8080
    targetPort: 5000
    protocol: TCP
    name: http
