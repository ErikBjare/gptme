apiVersion: v1
kind: ConfigMap
metadata:
  name: gptme-agent-config
  labels:
    app: gptme-agent-label
data:
  # Server configuration
  SERVER_HOST: "0.0.0.0"
  SERVER_PORT: "5000"
  CORS_ORIGIN: "*"

  # Additional configuration can be added here
  # MODEL: ""
  # TOOLS: ""
